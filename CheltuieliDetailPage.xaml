<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BugetPersonal.CheltuieliDetailPage"
             Title="{Binding PageTitle}">
    <ScrollView>
        <VerticalStackLayout Spacing="10" Padding="20">
            <Label Text="Descriere:" />
            <Entry Text="{Binding Cheltuieli.Description}" Placeholder="Descrierea cheltuielii" />

            <Label Text="Sumă:" />
            <Entry Text="{Binding Cheltuieli.Amount}" Keyboard="Numeric" Placeholder="0.00" />

            <Label Text="Categorie:" />
            <CollectionView x:Name="categoriesCollection" 
                       ItemsSource="{Binding Categories}"
                       SelectionMode="Single"
                       SelectedItem="{Binding SelectedCategory}"
                       HeightRequest="120"
                       SelectionChanged="OnCategorySelected">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Horizontal" ItemSpacing="10" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="5">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Frame Grid.Row="0" 
                               BorderColor="LightGray" 
                               CornerRadius="5" 
                               Padding="5"
                               WidthRequest="60" 
                               HeightRequest="60" 
                               HorizontalOptions="Center">
                                <Image Source="{Binding IconSource}" 
                                   WidthRequest="40" 
                                   HeightRequest="40" />
                            </Frame>
                            <Label Grid.Row="1" 
                               Text="{Binding Name}" 
                               HorizontalOptions="Center" 
                               FontSize="12" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Label Text="Data:" />
            <DatePicker Date="{Binding Cheltuieli.Date}" />

            <HorizontalStackLayout Spacing="10" HorizontalOptions="Center" Margin="0,20,0,0">
                <Button Text="Salvează" Clicked="OnSaveClicked" BackgroundColor="Green" TextColor="White" />
                <Button Text="Șterge" Clicked="OnDeleteClicked" BackgroundColor="Red" TextColor="White"  IsVisible="{Binding IsExistingExpense}"
                />
                <Button Text="Anulează" Clicked="OnCancelClicked" />
            </HorizontalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>